<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="guessWordTheme" parent="android:Theme.Holo">
        <item name="guessWordStyle">@style/guessWordStyle</item>
    </style>

    <!-- Define the custom button style itself. Make sure
         to inherit from an existing button style. -->
    <style name="guessWordStyle" parent="Base.TextAppearance.AppCompat.Large">
        <item name="android:textColor">#000</item>
        <item name = "android:textAllCaps">true</item>
        <item name = "android:textSize">35sp</item>
    </style>

</resources>
